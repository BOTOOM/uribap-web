# Research: Recipes and Ingredients Web

- Use Server Components for initial list/detail data and server-only BFF calls.
- Use client islands for filters/forms; do not duplicate API quantity/domain logic.
- Use generated `openapi-typescript`/`openapi-fetch` types after API 003 publishes its snapshot.
- Use existing foundation states and design tokens; no generic card grid or food imagery.
