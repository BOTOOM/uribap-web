# Quickstart: Demand Forecast Web

```bash
pnpm api:check
pnpm lint
pnpm typecheck
pnpm test
pnpm test:e2e
pnpm test:a11y
pnpm build
pnpm audit --audit-level=high
pnpm licenses:check
pnpm performance:check
docker compose up -d --build
```

Verify the forecast table at 375/768/1024/1440, previous/next-week navigation, shortfall highlighting, empty state without approved plans, forbidden state, keyboard operation, and reduced motion.

## Evidence (2026-09-21)

- `pnpm api:check`: PASS — generated schema matches the API 006 snapshot including `DemandForecastResponse`/`DemandForecastLine`.
- `pnpm lint`, `pnpm typecheck`: PASS (`/forecast` typed route generated by the build).
- `pnpm test`: 25 Vitest cases pass (forecast table, week window math, planning, contract metadata).
- `pnpm test:e2e`: 8 passed, 7 explicitly skipped pending a reusable authenticated local session (same opt-in posture as phases 004–005).
- `pnpm build`: PASS — `/forecast` page and `/api/forecast/demand` BFF handler compile.
- `pnpm audit --audit-level=high`: no known vulnerabilities.
- `pnpm licenses:check`: PASS.
- `pnpm performance:check`: 1,235,350 / 2,000,000 bytes.
